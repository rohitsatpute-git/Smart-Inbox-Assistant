# Sample outputs

`message_01.json` … `message_20.json` and `timings.json` are heuristic-pipeline runs (no `GEMINI_API_KEY` in this environment). Re-run with Gemini for higher-quality summaries:

```bash
python scripts/dump_sample_outputs.py
```

(requires `uvicorn` on port 8000).
